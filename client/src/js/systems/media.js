export default class MediaSystem {

    constructor (world) {

        this.world = world

    }

    init (component) {

        let prop = component.props.media

        if (prop.playPause) {

        } else if (prop.title) {

        } else if (prop.queue) {

        } else if (prop.add) {
            
        } else if (prop.remove) {

        }

        return {
            
        }

    }
    
}