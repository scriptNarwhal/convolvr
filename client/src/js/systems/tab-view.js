export default class TabViewSystem {
    constructor (world) {
        this.world = world
    }

    init (component) { // make component toggle visible when tab is activated
        let prop = component.props.tabView,
            state = {}

        return state
    }
}

