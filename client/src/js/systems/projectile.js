export default class ProjectileSystem {
    constructor (world) {
        this.world = world
    }

    init (component) { 
        let prop = component.props.projectile,
            state = {}

        return state
    }
}

