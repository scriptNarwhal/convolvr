export default class DatGUIVRPluginSystem {

    constructor (world) {
        this.world = world
    }

    init (component) { 
        
        // implement binding guis and controllers to props and state
        // implement updating entities for changes requiring re-init

        return {
            
        }
    }
}