export default class ObjSystem { // allows use of imported .obj meshes

    constructor (world) {

        this.world = world
        
    }

    init (component) { 
        
        return {
           
        }
        
    }
}