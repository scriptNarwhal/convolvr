export default class ConditionSystem {

    constructor ( world ) {

        this.world = world

    }

    init ( component ) {

        let prop = component.props.condition,
            state = {}

        return state
        
    }
}