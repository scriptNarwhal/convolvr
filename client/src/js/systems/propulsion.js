export default class PropulsionSystem {
    constructor (world) {
        this.world = world
    }

    init (component) { 
        let prop = component.props.propulsion,
            state = {}

        return state
    }
}

