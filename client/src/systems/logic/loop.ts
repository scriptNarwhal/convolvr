export default class LoopSystem {

    constructor ( world ) {

        this.world = world

    }

    init(component: Component) {

        let attr = component.attrs.loop,
            state: any = {}

        return state

    }
}