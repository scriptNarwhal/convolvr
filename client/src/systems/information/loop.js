export default class LoopSystem {

    constructor ( world ) {

        this.world = world

    }

    init ( component ) {

        let prop = component.props.loop,
            state = {}

        return state

    }
}