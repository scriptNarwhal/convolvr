export default class NPCSystem { // for client side aspects of npcs

    constructor ( world ) {

        this.world = world
    }

    init ( component ) {

        let attr = component.attrs.npc

        
         
        return {

        }
    }
}