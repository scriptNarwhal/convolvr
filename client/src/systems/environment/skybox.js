
export default class SkyboxSystem {
    
    constructor ( world ) {
        this.world = world
    }

    init ( component ) {

    
        return {
           
        }
    }

    createSkybox ( config ) {

        let skybox = null
        // implement
        return skybox
    }
}